# Filio — מסמך אפיון

**גרסה:** 2.9.1 | **עדכון:** 2026-09-18 | **פלטפורמה:** Windows 10/11 x64

**מקור ופרסום:** `github.com/ofirshudari1-ship-it/filio` — כל גרסה מתויגת (`vX.Y.Z`) עם קובץ
ההתקנה כ-asset בינארי ב-Release. בדיקת עדכונים: `UpdateService.CheckGitHubReleaseAsync` בודקת
פעם אחת בכל הפעלה מול `GET /repos/.../releases/latest` הפומבי, ומציגה התראה בלבד (לא מורידה/
מתקינה) אם יש גרסה חדשה — בנוסף לזרימת ה-`UpdateFeedUrl` הישנה (manifest + התקנה שקטה, למתקדמים).

## מה הכלי עושה

Filio הוא כלי רקע ל-Windows שמתייק קבצים אוטומטית מתיקיית ההורדות (ותיקיות נוספות לפי בחירה) לתוך מבנה מסודר לפי לקוח / שנה / סוג מסמך.

## למי

עצמאים ועסקים קטנים שמוצאים את תיקיית ההורדות שלהם כאוטית — מלאה בחשבוניות, חוזים, מצגות ומסמכי לקוחות שמעורבבים בלי סדר.

## יכולות עיקריות (v2.8.0)

| יכולת | פרטים |
|---|---|
| ניטור תיקיות | `FileSystemWatcher` על תיקיות מוגדרות על-ידי המשתמש |
| סיווג מסמכים | PDF, DOCX, תמונות — לפי לקוח/שנה/סוג |
| OCR מקומי | Tesseract — עברית + אנגלית, ללא שרת |
| זיהוי כפילויות | SHA-256 hash-based |
| מיון קובצי התקנה | exe/msi → `_Installers\שנה` (כבוי כברירת מחדל) |
| בדיקת Defender | MpCmdRun.exe -Scan לפני כל תיוק |
| ביטול פעולה | undo עבור הקובץ האחרון |
| בדיקה לפני תיוק (אופציונלי) | קובץ שלא זוהה בו שום לקוח מוכר נשאר במקום וממתין לאישור ידני, במקום להיכנס אוטומטית ל"לקוח לא ידוע" - כבוי כברירת מחדל |
| תיקון + למידה | "תקן / תייק…" ביומן הפעילות מאפשר לבחור לקוח/סוג נכונים לקובץ (גם קובץ שממתין לבדיקה, גם קובץ שכבר תויק לא נכון) ואופציונלית ללמד את Filio כינוי חדש ללקוח כדי שקבצים דומים יזוהו אוטומטית בעתיד |
| לוקליזציה | עברית + אנגלית, RTL/LTR |
| עדכון אוטומטי | מתעדכן שקט ב-/VERYSILENT |

## מה הכלי לעולם לא עושה

- **לא מוחק קבצים** — רק מעביר/מעתיק ורושם ביומן
- לא שולח נתונים לענן
- לא עובד ללא Windows Defender (יפסח ויתייק בכל מקרה)

## מבנה תיקיות פלט

```
[תיקיית יעד]\
├─ [לקוח]\[שנה]\[סוג מסמך]\     ← ברירת מחדל: לקוח/שנה/סוג
├─ _Installers\[שנה]\              ← קובצי exe/msi (אם הופעל)
├─ _Possible Duplicates\           ← כפילויות לבדיקה ידנית
```

## מצבים חסרים / פיתוח עתידי

- [ ] זיהוי שפה אוטומטי מתוכן הקובץ עצמו (לא רק OCR)
- [ ] ממשק web מקומי (dashboard)
- [ ] תמיכה ב-NAS/OneDrive/Google Drive כיעד
- [ ] API לאינטגרציה עם כלים חיצוניים

## דרישות מערכת

- Windows 10 (1809+) או Windows 11
- .NET 8 Runtime (מוטמע ב-installer)
- Windows Defender פעיל — רצוי אך לא חובה
- 150 MB מקום פנוי

## אשף ההתקנה (2.9.0)

מ-2.9.0, ה-installer הוא אשף WinForms מותאם אישית (`build/installer/Setup.cs`/`Setup.csproj`),
לא Inno Setup — ראו CHANGELOG.md 2.9.0 לפירוט המלא. בקצרה: עיצוב כהה במיתוג Filio (טיל/כתום),
כל פקד צבוע במפורש (לא נשען על סגנון Windows גנרי), בחירת שפה (אנגלית/עברית RTL), עמוד
הסבר/שיווק, עמוד רישיון נפרד, בחירת מיקום התקנה, והתקנה גם משמשת כתוכנת ההסרה שלה
(`Filio-Setup.exe --uninstall`). `Filio-Setup.exe --selftest` מריץ בדיקת עשן ללא ממשק.
