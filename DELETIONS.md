# DELETIONS.md — Filio

רישום כל מחיקת קובץ, כנדרש ב-STANDARDS.md, לפני ביצוע בפועל.

## 2026-09-14

| קובץ | סיבה | סטטוס |
|---|---|---|
| `docs/מסמך-אפיון.html` | מסמך אפיון ישן בפורמט HTML, קודם ל-`SPEC.md` שנוצר ב-2026-09-14. כל התוכן שלו (מה הכלי עושה, למי, יכולות) כלול כעת ב-`SPEC.md` בפורמט מתוחזק. שמירת שני מסמכי אפיון מקבילים יוצרת סיכון שיתבדרו זה מזה. | נמחק |
| `docs/` (התיקייה, אם התרוקנה) | תיקייה ריקה לאחר מחיקת הקובץ היחיד שהיה בה | נמחק אם ריקה |
| `build/FilioSetup.exe` | עותק כפול בדיוק (byte-identical) של `FilioSetup.exe` שבשורש — תוצר ביניים של תהליך הבנייה שמועתק לשורש על ידי `build.ps1`. לפי STANDARDS.md סעיף "מבנה תיקייה אחיד", `build/` מיועד לסקריפטים/כלים, לא לעותק שני של קובץ ההתקנה הסופי. השארתו רק מכפילה שטח דיסק (58MB) ללא תועלת. | נמחק בכל build חדש (build.ps1 עודכן לנקות) |

**הערה:** שני הקבצים הם build artifacts הניתנים לשחזור מלא על ידי הרצת `build.ps1` מחדש — עומדים בקריטריון "ברור חד-משמעית שזה build output בר-שחזור מלא" מ-STANDARDS.md, כך שאין צורך באישור נפרד לפני מחיקה.

## 2026-09-15 — מיתוג מחדש FileFox → Filio

| קובץ | סיבה | סטטוס |
|---|---|---|
| `app/Filio.App/Assets/filefox.ico` + `filefox-{16,24,32,48,64,128,256}.png` | לוגו המותג הישן (שועל) — הוחלף בלוגו Filio החדש (תיקייה+וי+נקודת "i" טאלית) לפי בקשת המשתמש להתרחק מדמיון ל-Firefox. | נמחק, הוחלף ב-`filio.ico`/`filio-*.png` |
| `design-source/filefox_logo_master.svg` + `filefox_logo_master_512.png` | קובצי המקור של הלוגו הישן — שוכפלו ללוגו Filio חדש לגמרי (`filio_logo_master.svg`/`_512.png`), לא רק שינוי שם. | נמחק |

**הערה:** שינוי מיתוג מלא ביוזמת המשתמש (הדמיון ל-Firefox היה בעייתי) — כל הקבצים החדשים תחת אותה תיקיית `design-source/` עם שם עקבי `filio_*`.

## 2026-09-15 — ביקורת תאימות STANDARDS.md מלאה (v2.7.0→2.7.1)

| קובץ | סיבה | סטטוס |
|---|---|---|
| `FilioSetup.exe` (שורש) | קובץ ההתקנה שהיה בשורש נבנה תחת v2.7.0 ולפי שם ישן (`FilioSetup.exe`) שלא תואם למוסכמה הנדרשת ב-STANDARDS.md סעיף 1/11.1 (`<ToolName>-Setup-<version>.exe`, לעולם לא "Setup.exe" גנרי). `installer/setup.iss` הועבר ל-`build/installer/setup.iss` ועודכן לפלט `Filio-Setup-<version>.exe`, והגרסה עלתה ל-2.7.1 (ראו CHANGELOG). **אין סביבת Inno Setup (ISCC.exe) זמינה בסביבת העבודה הזו** כדי לבנות מחדש את קובץ ההתקנה בפועל — השארת הקובץ הישן (עם שם/גרסה לא-תואמים לקוד המקור העדכני) הייתה מפרה את הכלל שאסור "installer ישן ליד קוד מקור מעודכן", ולכן הוא נמחק במקום זאת. **פעולה נדרשת מהבעלים:** להריץ `build.ps1` על מחשב עם Inno Setup 6 מותקן כדי לייצר את `Filio-Setup-2.7.1.exe` לפני הפצה. | נמחק — ניתן לשחזור מלא ע"י הרצת `build.ps1` על מכונה עם Inno Setup |

**הערה:** קובץ בר-שחזור מלא (`dotnet publish` + Inno Setup compile, שני הצעדים מתועדים ב-`build.ps1`), עומד בקריטריון המחיקה ללא אישור נפרד.
