<Window x:Class="Filio.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="260" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">
    <Border CornerRadius="16" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F5348" Offset="0"/>
                <GradientStop Color="#1E7F72" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40,0">
                <Image Source="/Assets/filio-128.png" Width="64" Height="64" Margin="0,0,0,12"/>
                <TextBlock Text="Filio" FontSize="32" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" FontFamily="Segoe UI"/>
                <TextBlock x:Name="TaglineText" Text="{DynamicResource AppTagline}" FontSize="14"
                           Foreground="#DFF3EF" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                <TextBlock x:Name="VersionText" Text="v2.7.0" FontSize="12"
                           Foreground="#99C5BF" HorizontalAlignment="Center" Margin="0,4,0,24"/>
                <ProgressBar x:Name="SplashProgress" Height="3" Width="200"
                             Background="#20FFFFFF" Foreground="#DB8A1F"
                             BorderThickness="0" Value="0" Maximum="100"/>
                <TextBlock x:Name="StatusText" Text="Loading…" FontSize="11"
                           Foreground="#99C5BF" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
